# 패키지 전역에서 쓰는 예외 타입 모음.
# web/main.py(패키지 바깥)에서도 이 타입을 보고 "LLM이 일시적으로 안 된다"는 상황을
# 다른 에러(버그, 잘못된 입력 등)와 구분해서 다른 HTTP 상태코드로 응답할 수 있게 합니다.


class LLMUnavailableError(Exception):
    """LLM 호출을 여러 번 재시도했는데도 계속 실패했을 때 발생시키는 예외."""
